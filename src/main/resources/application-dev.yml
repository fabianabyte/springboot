debug: true

server:
    port: 8085

app:
  message: Arquivo de propriedades para ambiente de desenvolvimento da ${spring.application.name}

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa